stages:
  - build

dind-build:
  stage: build

  image:
    name: docker:stable
  services:
   - name: docker:dind

  variables:
    DOCKER_HOST: tcp://localhost:2375/
    DOCKER_DRIVER: overlay2
    DOCKER_TLS_CERTDIR: ""

  script:
    - docker build -t "image:test" .
    - docker ps